filters:
  - pandoc/filters/pandoc-ling.lua  # Path relative to workspace root
  - pandoc-crossref
  # - citeproc # This is now handled by the --citeproc flag

# Default metadata values that can be overridden by documents
metadata:
  # Bibliography and citation style are now handled by the --citeproc flag
  # and project-specific metadata.
  # reference-section-title: "Bibliography"
  link-citations: true
  linkReferences: true # For pandoc-crossref to link references

# Output file settings
number-sections: true